apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-ui
  namespace: todo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-ui
  template:
    metadata:
      labels:
        app: react-ui
    spec:
      containers:
      - name: react-ui
        image: k8sdemoproject.azurecr.io/react-ui:latest
        ports:
        - containerPort: 80
        imagepullsecrets:
        - name: acr-secret
